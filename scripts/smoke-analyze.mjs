import{execFile}from"node:child_process";import{mkdtemp,readFile}from"node:fs/promises";import{tmpdir}from"node:os";import{join}from"node:path";import{promisify}from"node:util";const exec=promisify(execFile);const root=await mkdtemp(join(tmpdir(),"anthesis-cli-smoke-"));await exec("bash",["test/fixtures/create-histories.sh",root]);const output=join(root,"analysis.json");await exec("node",["dist/cli/index.js","analyze",join(root,"steady-growth"),"--output",output,"--full-analysis"]);const value=JSON.parse(await readFile(output,"utf8"));if(value.schemaVersion!==1||value.commits.length<2||value.warnings[0]?.code!=="GIT_ONLY_LIMITATIONS")throw new Error("analyze CLI did not observably produce full analysis");
