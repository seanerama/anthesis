const enabled=import.meta.env.ANTHESIS_VIEWER_ENABLED==="true";const portrait=document.querySelector<HTMLElement>("#portrait")!;const disabled=document.querySelector<HTMLElement>("#disabled")!;if(enabled){portrait.hidden=false;fetch("/portrait.provenance.json").then(r=>r.json()).then((p:{canonicalModelSha256:string})=>{document.querySelector("#provenance")!.textContent=p.canonicalModelSha256;});}else disabled.hidden=false;
