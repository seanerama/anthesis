import type { CanonicalRepositoryV1 } from "../../canonical/model.js";
import type { BotanicalSceneV1 } from "../../botany/model.js";
import { canonicalJson, sha256 } from "../../shared/canonical-json.js";
import { layout } from "../../geometry/index.js";
export type SvgProvenanceV1={schemaVersion:1;project:"Anthesis";repository:string;snapshotSha:string;periodStart:string;periodEnd:string;analysisMode:"git-only"|"forge-enriched";provider?:string;scalingMode:"repository-relative"|"globally-comparable";rendererVersion:string;grammarVersion:string;style:string;configurationHash:string;seed:string;canonicalModelSha256:string;botanicalSceneSha256:string};
const esc=(s:string)=>s.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");
export function renderSvg(c:CanonicalRepositoryV1,b:BotanicalSceneV1,canonicalBytes:string,botanicalBytes:string):{svg:string;provenance:string}{const p:SvgProvenanceV1={schemaVersion:1,project:"Anthesis",repository:c.repository.identity,snapshotSha:c.repository.snapshotSha,periodStart:c.period.start,periodEnd:c.period.end,analysisMode:c.analysis.mode,scalingMode:b.generation.scalingMode,rendererVersion:"stage-0-v1",grammarVersion:b.grammarVersion,style:"herbarium",configurationHash:c.analysis.configHash,seed:b.generation.seed,canonicalModelSha256:sha256(canonicalBytes),botanicalSceneSha256:sha256(botanicalBytes)};const provenance=canonicalJson(p);const g=layout(b);const flowers=g.flowers.map(f=>`  <g id="${esc(f.id)}" data-source-kind="tag" data-source-ref="${esc(f.source)}"><circle cx="${f.x}" cy="${f.y}" r="${f.radius}" fill="#d45b78"/><circle cx="${f.x}" cy="${f.y}" r="18" fill="#f2c14e"/><title>Tag ${esc(f.source)}</title></g>`).join("\n");const svg=`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 1000" role="img" aria-labelledby="title description" data-anthesis-contract="svg-provenance-v1">\n  <title id="title">Anthesis repository portrait</title>\n  <description id="description">A main stem and tagged flower derived from Git history</description>\n  <metadata id="anthesis-provenance">${esc(provenance)}</metadata>\n  <rect width="800" height="1000" fill="#f8f3e7"/>\n  <path id="main-stem" d="M ${g.stem.x} ${g.stem.y1} C 350 680 450 440 ${g.stem.x} ${g.stem.y2}" fill="none" stroke="#315b3a" stroke-width="24" stroke-linecap="round"/>\n${flowers}\n</svg>\n`;return{svg,provenance:`${provenance}\n`};}
