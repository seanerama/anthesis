import type {BotanicalSceneV2} from "../../../botany/v2/model.js";
import {buildCandidateGeometry,evaluateSceneCandidate,generateCandidates} from "../../../geometry/v2/index.js";
import {pathData,number,xml} from "./serializer.js";
export function renderCandidateContactSheet(scene:BotanicalSceneV2):string{const ranked=generateCandidates(scene).map(c=>({c,e:evaluateSceneCandidate(scene,c)})).filter(x=>x.e.hardViolations.length===0).sort((a,b)=>a.e.totalScore-b.e.totalScore||a.c.index-b.c.index).slice(0,4);const panels=ranked.map((x,i)=>{const g=buildCandidateGeometry(scene,x.c),paths=[...g.layers.values()].flat().filter(p=>p.kind!=="mark-field").map(p=>`<path d="${pathData(p.commands)}" fill="${p.kind==="closed-path"?"#f8f8f4":"none"}" stroke="#202820" stroke-width=".7" vector-effect="non-scaling-stroke"/>`).join(""),dx=(i%2)*320,dy=Math.floor(i/2)*410;return `<g transform="translate(${dx} ${dy}) scale(.48)">${paths}</g><text x="${dx+10}" y="${dy+398}" font-size="11" font-family="sans-serif">candidate ${x.c.index} · score ${x.e.totalScore}</text>`;}).join("");return`<?xml version="1.0" encoding="UTF-8"?>\n<svg xmlns="http://www.w3.org/2000/svg" width="640" height="820" viewBox="0 0 640 820" role="img" aria-label="Top four deterministic composition candidates"><rect width="640" height="820" fill="#f4eedf"/>${panels}<metadata>${xml(`non-authoritative; candidates=${ranked.map(x=>x.c.index).join(",")}`)}</metadata></svg>\n`;}
